/**
 * Button component barrel.
 *
 * Re-exports the React button primitive, its props, and the shared variant
 * factory used by call sites that need matching button styling.
 *
 * @module button
 */
export { Button } from './button.component';
export type { ButtonProps } from './button.component';
export { buttonVariants } from './button.variants';
