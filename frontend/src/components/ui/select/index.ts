/**
 * Select component barrel — a custom dropdown that replaces the native
 * `<select>` popup with a portalled, fully-styled listbox.
 *
 * @module select
 */
export { Select } from './select.component';
export type { SelectOption } from './select.component';
